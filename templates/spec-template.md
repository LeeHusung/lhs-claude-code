# {프로젝트명} — 설계 스펙

## 1. 프로젝트 요약

{한 문단으로 프로젝트의 목적, 대상 사용자, 핵심 가치를 기술한다.}

## 2. 기술 스택

| 영역 | 선택 | 선택 이유 |
|------|------|----------|
| Frontend | {프레임워크/라이브러리} | {프로젝트 특성에 기반한 선택 근거} |
| Backend | {프레임워크/런타임} | {선택 근거} |
| Database | {DB 종류} | {선택 근거} |
| Styling | {CSS 방식} | {선택 근거} |
| 기타 | {필요 시} | {선택 근거} |

## 2.5. 디자인 비전

- **디자인 컨셉**: {앱의 시각적 정체성을 한 문장으로. 예: "어두운 배경 위의 네온 데이터 시각화 — 사이버펑크 컨트롤룸 느낌"}
- **색상 방향**: {구체적 분위기와 색조. 예: "차가운 블루-그레이 기반에 민트 그린 액센트"}
- **절대 사용하지 말 것**:
  - {AI 생성 패턴 거부 목록. 예: 보라색 그래디언트, 동일 카드 3열, 제네릭 히어로}
- **레퍼런스 해석**: {요구사항의 레퍼런스에서 추출한 핵심 디자인 요소}

## 3. 기능별 상세 스펙

### 3.1 {기능명} — 우선순위: {필수/권장/선택}

**행동 설명**:
{사용자가 무엇을 하면 시스템이 어떻게 반응하는지 구체적으로 기술. 입력 → 처리 → 출력 흐름.}

**UI 와이어프레임 (텍스트)**:
```
{화면 레이아웃을 텍스트로 표현}
┌─────────────────────────────┐
│ Header: 로고 | 네비게이션    │
├─────────────────────────────┤
│ [컴포넌트 설명]              │
│ [버튼/입력 필드 설명]         │
└─────────────────────────────┘
```

**수락 기준** (Evaluator가 이 기준으로 검증):
- [ ] {구체적 동작 조건 1}
- [ ] {구체적 동작 조건 2}
- [ ] {에러 없이 동작}

**엣지 케이스** (데모 범위 내):
- {빈 입력 시 동작}
- {경계값 동작}

### 3.2 {기능명} — 우선순위: {필수/권장/선택}

{위와 동일한 형식으로 반복}

## 4. 화면별 레이아웃

### 4.1 {화면명}

**레이아웃 구조**:
```
{텍스트 기반 레이아웃 설명}
```

**포함 컴포넌트**:
| 컴포넌트 | 역할 | 위치 |
|---------|------|------|
| {컴포넌트명} | {역할 설명} | {헤더/사이드바/메인 등} |

**네비게이션 흐름**:
- 이 화면 → {다음 화면}: {트리거 동작}
- {이전 화면} → 이 화면: {트리거 동작}

### 4.2 {화면명}

{위와 동일한 형식으로 반복}

## 5. 데이터 모델

### 엔티티 목록

| 엔티티 | 필드 | 타입 | 설명 |
|--------|------|------|------|
| {엔티티명} | id | number | PK |
| | {필드명} | {타입} | {설명} |

### 엔티티 관계
- {엔티티A} 1:N {엔티티B} — {관계 설명}

## 6. API 엔드포인트 (해당 시)

| Method | Path | 설명 | Request Body | Response |
|--------|------|------|-------------|----------|
| {GET/POST/...} | {/api/...} | {설명} | {필요 시 구조} | {응답 구조} |

## 7. 데모 시나리오

데모 발표 시 보여줄 흐름을 순서대로 기술합니다.

1. **시작**: {첫 화면 진입 → 보여줄 것}
2. **핵심 기능 1**: {동작 시연 순서}
3. **핵심 기능 2**: {동작 시연 순서}
4. **마무리**: {마지막으로 보여줄 화면/기능}
